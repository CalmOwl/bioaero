---
import type { Screening } from "../types";

interface Props {
  screening: Screening;
  filmTitle: string;
  screeningId: string;
}

const { screening, filmTitle, screeningId } = Astro.props;
---

<li>
  <p>
    <span>{screening.date}</span>
    <span>{screening.time}</span>
    <span>{screening.place}</span>
  </p>
  <div class="screening-buttons-wrapper">
    <button
      class="screening-wishlist"
      data-id={screeningId}
      data-film={filmTitle}
      data-date={screening.date}
      data-time={screening.time}
      data-place={screening.place}
      data-tickets-url={screening.ticketsUrl}
    >
      <span class="screening-wishlist__icon"></span>
      <span class="screening-wishlist__label">хочу пойти</span>
    </button>
    <a class="film-tickets" href={screening.ticketsUrl}>
      <span class="icon-tickets"></span>
      билеты
    </a>
  </div>
</li>

<style>
  .screening-buttons-wrapper {
    display: flex;
    gap: 1rem;
    flex-grow: 1;
    flex-wrap: wrap;
  }

  .screening-wishlist {
    border: none;
    display: flex;
    gap: 8px;
    align-items: center;
    justify-content: center;
    max-width: fit-content;
    border-radius: 1rem;
    padding: 0.7rem 0.8rem;
    background-color: var(--color-grey);
    flex-shrink: 0;
    flex-grow: 1;
    transition: 0.5s;

    @media (hover: hover) {
      &:hover {
        background-color: var(--color-accent-dark);
        color: var(--color-white);
      }
    }
  }

  .screening-wishlist:focus-visible,
  .film-tickets:focus-visible {
    background-color: var(--color-accent-dark);
    color: var(--color-white);
  }

  .screening-wishlist:has(.is-active) {
    background-color: var(--color-white);
  }

  .screening-wishlist__icon {
    display: block;
    width: 26px;
    height: 26px;
    mask-repeat: no-repeat;
    mask-position: center;
    mask-size: contain;
    mask-image: url("/bioaero/svg/wishlist-star.svg");
    background-color: var(--color-black);
    transition: 0.3s;

    &.is-active {
      mask-image: url("/bioaero/svg/favourites.svg");
    }

    .screening-wishlist:focus-visible & {
      background-color: var(--color-grey);
    }

    @media (hover: hover) {
      .screening-wishlist:hover & {
        background-color: var(--color-grey);
        transform: rotateZ(10deg);
      }
    }

    .screening-wishlist:active & {
      transform: rotateZ(360deg);
    }
  }

  .screening-wishlist__label {
    transition: 0.3s;

    .screening-wishlist.is-saved:focus-visible & {
      color: var(--color-grey);
    }

    @media (hover: hover) {
      .screening-wishlist.is-saved:hover & {
        color: var(--color-grey);
      }
    }
  }

  .film-tickets {
    display: flex;
    gap: 8px;
    align-items: center;
    justify-content: center;
    max-width: fit-content;
    border-radius: 1rem;
    padding: 0.7rem 0.8rem;
    background-color: var(--color-grey);
    flex-grow: 1;
    transition: 0.3s;

    @media (hover: hover) {
      &:hover {
        background-color: var(--color-accent-dark);
        color: var(--color-white);
      }
    }
  }

  .icon-tickets {
    display: block;
    width: 26px;
    height: 26px;
    mask-repeat: no-repeat;
    mask-position: center;
    mask-size: contain;
    mask-image: url("/bioaero/svg/ticket.svg");
    background-color: var(--color-black);
    transition: 0.3s;

    .film-tickets:focus-visible & {
      background-color: var(--color-grey);
    }

    @media (hover: hover) {
      .film-tickets:hover & {
        background-color: var(--color-grey);
        transform: rotate(270deg);
      }
    }
  }
</style>
